# MagicSquare_1422 — D-LOC-01 RED ④ pytest.fail 스켈레톤

| 항목 | 내용 |
|------|------|
| **프로젝트** | MagicSquare_1422 |
| **단계** | ARRR A단계 RED ④ — D-LOC-01 `pytest.fail` 스켈레톤 |
| **보고서 생성일** | 2026-06-25 |
| **목적** | RED ③ 설계표 기준으로 `tests/`에 D-LOC-01 스켈레톤만 작성하고 의도적 FAIL을 확인 |

## 0. 세션 메타 (Step A)

| 항목 | 값 |
|------|-----|
| **Phase** | `red` |
| **Test ID** | `D-LOC-01` |
| **Command** | `/red-skeleton` → `/export` |
| **Track** | `Logic` (Layer: `entity`) |

### pytest (실행 결과)

```
python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v

collected 1 item
FAILED tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major
Failed: RED: D-LOC-01 — 구현 없음, 의도적 실패

1 failed in 0.18s
```

## 1. 요약

`/red-skeleton`으로 **D-LOC-01** RED ④ 스켈레톤을 작성했다. `tests/conftest.py`에 PRD §3.2 슬라이드 예시 기반 `grid_g1` 픽스처(0 두 개, row-major 4×4)를 추가하고, `tests/entity/test_d_loc_01.py`에 AAA 주석과 `pytest.fail` 한 줄만 포함한 `test_d_loc_01_blank_coords_row_major`를 생성했다.

When 주석은 사용자 지정대로 `find_blank_coords(grid_g1)` 호출을 명시했고, Then 주석은 `[(2,2),(3,3)]` (1-index, row-major) 기대를 문서화했다. `src/`는 수정하지 않았으며, 대상 테스트는 의도대로 `pytest.fail`로 실패했다.

## 2. Phase별 STEP

### RED (`Phase: red`)

| STEP | 내용 |
|------|------|
| **RED-③ 플랜** | N/A — 직전 세션(Report/04)에서 확정 |
| **RED-④ 스켈레톤** | `tests/conftest.py` · `tests/entity/test_d_loc_01.py` 생성; `pytest.fail("RED: D-LOC-01 — 구현 없음, 의도적 실패")` |

### GREEN (`Phase: green`)

N/A

### REFACTOR (`Phase: refactor`)

N/A

## 3. 핵심 결정·산출물

### 산출물

| 파일 | 작업 | 비고 |
|------|------|------|
| `tests/conftest.py` | 신규 | `grid_g1` 픽스처 (G1 격자) |
| `tests/entity/test_d_loc_01.py` | 신규 | D-LOC-01 RED 스켈레톤 |
| `Report/05.REPORT.md` | 신규 | 본 보고서 |
| `Prompting/05.Export-Transcript.md` | 신규 | 세션 Transcript |

### RED ③ 대비 차이 (기록)

| 항목 | RED ③ (Report/04) | RED ④ (본 세션) |
|------|-------------------|-----------------|
| 대상 함수 | `blank_coords(grid)` | `find_blank_coords(grid)` (사용자 템플릿) |
| Then 좌표 | `[(1,3), (2,2)]` (0-index) | `[(2,2),(3,3)]` (1-index, 주석) |
| fail 메시지 | row-major 빈칸 좌표 2개 반환 | 구현 없음, 의도적 실패 |

> GREEN 단계에서 함수명·좌표 체계를 RED ③·④ 중 하나로 정합 필요.

### pytest 스냅샷

| 항목 | 값 |
|------|-----|
| 명령 | `python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v` |
| 결과 | `1 failed` (의도적 `pytest.fail`) |
| `src/` | 미수정 |

## 4. 다음 단계

1. **`/green-minimal`** — `find_blank_coords`(또는 `blank_coords`) 최소 구현; `pytest.fail` → assert 교체
2. **D-LOC-02·03** — 후속 RED 사이클 (동일 파일)
3. **`tests/entity/constants.py`** — GREEN 시 `GRID_SIZE` 등 상수 SSOT 생성 검토

관련 Transcript: [Prompting/05.Export-Transcript.md](../Prompting/05.Export-Transcript.md)

*본 문서는 Report/05.REPORT.md — D-LOC-01 RED ④ pytest.fail 스켈레톤 세션 보고서입니다.*
